@using App.Core.Entities
<div class="content-wrapper">
    <div class="row">

        <div class="col-lg-12 stretch-card">
            <div class="card">
                <div class="card-body">
                    <h4 class="card-title">Category Table</h4>
                    <a class="nav-link" asp-action="Create" asp-controller="Category"><button type="button" class="btn btn-primary">Create</button></a>


                    <div class="table-responsive pt-3">
                        <table class="table table-bordered">
                            <thead>
                                <tr>
                                    <th>
                                        #
                                    </th>
                                    <th>
                                        Id
                                    </th>
                                    <th>
                                        Name
                                    </th>
                                    <th>
                                        Created Date
                                    </th>
                                    <th>
                                        Updated Date
                                    </th>
                                    <th>
                                        Change Buttons
                                    </th>
                                </tr>
                            </thead>
                            <tbody>
                                @{
                                    var count = 0;
                                }
                                @foreach (var item in ViewData["Categories"] as IQueryable<Category>)
                                {
                                    count++;
                                    <tr class="table-success">
                                        <td>
                                            @count
                                        </td>
                                        <td>
                                            @item.Id
                                        </td>
                                        <td>
                                            @item.Name
                                        </td>
                                        <td>
                                            @item.CreatedDate.ToString("MM/dd/yyyy HH:mm:ss")
                                        </td>
                                        <td>
                                            @item.UpdatedDate.ToString("MM/dd/yyyy HH:mm:ss")
                                        </td>
                                        <td>
                                            <a asp-action="Update" asp-controller="Category" asp-route-id="@item.Id"><button type="button" class="btn btn-warning">Update</button></a>
                                            <a asp-action="Detail" asp-controller="Category" asp-route-id="@item.Id"><button type="button" class="btn btn-success">Detail</button></a>
                                            <a asp-action="Delete" asp-controller="Category" asp-route-id="@item.Id"><button type="button" class="btn btn-danger">Delete</button></a>
                                        </td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>